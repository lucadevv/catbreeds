import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const primary = Color(0XFFE0EBEE);
  static const onPrimary = Color(0XFF505559);
  static const secondary = Color(0XFF30605F);
  static const onSecondary = Color(0XFFFFFFFF);
  static const tertary = Color(0XFFFFFFFF);
  static const onTertary = Color(0XFF010101);
  static const error = Color(0XFFFE0000);
  static const onError = Color(0XFFFFFFFF);
}
